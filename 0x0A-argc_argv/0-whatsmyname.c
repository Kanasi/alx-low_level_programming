#include "main.h"
#include <stdio.h>
/**
 * main - entry point to print name of the program
 * @argc: no of command
 * @argv: array of command line agr
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
