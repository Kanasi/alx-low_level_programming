#include "main.h"

/**
 * *cap_string - capitalize string
 * @s: string
 * Return: char
 */

char *cap_string(char *s)
{
	int i, j;

	i = 0;
	char t[] = "\t\n,;.!?\"(){}";

	while (*(s + i))
	{
		if (*(s + i) >= 'a' && *(s + i) <= 'z')
		{
			if (i == 0)
				*(s + i) -= 'a' - 'A';
			else
			{
				for (j = 0; j <= 12; j++)
				{
					if (a[j] == *(s + i - 1))
						*(s + i) -= 'a' - 'A';
				}
			}
		}
		i++;
	}
	return (s);
}
