#include <stdio.h>
/**
 * main - a function that prints numbers of base 10
 * Return:0 exit program
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf(n);
	}
	printf('\n');
	return (0);
}
